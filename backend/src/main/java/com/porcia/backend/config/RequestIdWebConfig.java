package com.porcia.backend.config;

import org.springframework.context.annotation.Configuration;

/**
 * RequestIdFilter is already registered as a @Component.
 * This config class intentionally left empty to avoid duplicate bean registration.
 */
@Configuration
public class RequestIdWebConfig {
}

